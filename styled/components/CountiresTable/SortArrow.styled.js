import styled     from 'styled-components';
import { colors } from '../../constants';

const SortArrowStyled = styled.div`
    color: ${colors.primary};
    display: flex;
    align-items: center;
    justify-content: center;
    margin-left: 2px;
`;

export default SortArrowStyled;
